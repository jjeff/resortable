dist/
node_modules/
legacy-sortable/
*.min.js
*.lock
# Generated by @semantic-release/changelog on every release.
CHANGELOG.md

# Agent prompt files. Line breaks and spacing are part of the prompt, so
# reformatting them can change behaviour rather than just presentation.
.claude/
.github/copilot-instructions.md

# Vendored from spec-kit. Formatting them only creates drift against upstream.
templates/

# Local scratch history, not part of the project.
.remember/
